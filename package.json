{
  "private": true,
  "version": "3.0.2",
  "license": "MIT",
  "description": "This package.json is here purely to control the version of the Action, in combination with https://github.com/JamesHenry/publish-shell-action",
  "scripts": {
    "build": "esbuild --bundle src/index.ts --outfile=dist/index.js --platform=node --target=node18 --sourcemap"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@octokit/action": "^6.0.5"
  },
  "devDependencies": {
    "@types/node": "^18",
    "esbuild": "^0.19.1",
    "typescript": "^5.1.6"
  }
}
